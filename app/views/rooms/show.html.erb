<div>
    <div></div>

    <div>
        <div>
            <%= @room.room_name %>
        </div>
        <div>
            <%= @room.room_address %>
        </div>
    </div>

    <div>
        <%= @room.room_introduction %>
    </div>

    <div>
        ¥ <%= @room.room_charge %> / 日
        
        <%= form_with model: @reservation, url:reservations_confirm_path, method: :post do |f| %>
            <%= f.label :start_date, "開始日" %>
            <%= f.date_field :start_day %>
            <%= f.label :end_date, "終了日" %>
            <%= f.date_field :end_day %>
            <%= f.label :people, "人数" %>
            <%= f.number_field :people %>

        <div>
            <ul>
                <li><%= link_to "予約する", "reservations/confirm" %></li>
            </ul>
        </div>

        <% end %>

    </div>

</div>
